# API Framework
fastapi==0.138.2
uvicorn[standard]==0.49.0

# Kafka (kafka-python for simpler thread-based consumer)
kafka-python==2.0.2

# LangChain + LangGraph + Ollama
langchain==0.3.7
langchain-core==0.3.17
langchain-community==0.3.7
langchain-ollama==0.2.0
langgraph==0.2.39

# Vector DB
qdrant-client==1.11.3

# Embeddings (converts text → vectors for RAG)
sentence-transformers==3.2.1

# Data validation (like Java DTOs)
pydantic==2.13.4

# Environment variables
python-dotenv==1.0.1

# HTTP client (used by mcp SDK internally)
httpx==0.27.2

# Model Context Protocol — official SDK (ClientSession, streamablehttp_client)
mcp==1.28.1

# PostgreSQL (optional — direct DB queries in MCP tools)
psycopg2-binary==2.9.9
